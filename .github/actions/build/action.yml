name: 'build'
description: 'test'
author: 'jshuangzl@gmail.com'

inputs: 
  myInput:
    description: 'Input to use'
    default: 'world'

runs:
  using: 'docker'
  image: 'Dockerfile'
  # args:
  #   - ${{ inputs.myInput }}